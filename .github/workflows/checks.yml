name: Checks

on:
  push:
    branches: [master, dev]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    env:
      # Tests never fetch fonts at runtime; the cache is pre-populated below.
      KOU_SKIP_FONT_FETCH: "1"
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2

      # Snapshot baselines are pixel-diffed, so the renderer must use the same
      # pinned fonts CI-to-CI and machine-to-machine. Pull the `fonts` archive
      # (built by fonts-release.yml) into the kou font cache before any test
      # runs. `fonts` is a rolling release, not versioned.
      - name: Prepare pinned test fonts
        run: |
          mkdir -p ~/.cache/kou/fonts
          gh release download fonts -R "$GITHUB_REPOSITORY" \
            -p 'fonts.zip' --clobber --output fonts.zip
          unzip -o fonts.zip -d ~/.cache/kou/fonts
          ls -la ~/.cache/kou/fonts/
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - run: cargo fmt --all -- --check
      - run: cargo clippy --all-targets --all-features -- -D warnings

      # Split the test run into two groups:
      #   1. Pure-render tests (no PTY, no child process) — fast, hermetic.
      #   2. PTY-driven tests — launch real shells, need Unix + a PTY.
      # Both share the same pinned-font cache prepared above.
      - name: Pure-render tests (lib + snapshots + kitty + mcp handshake)
        run: cargo test --all-features --lib --test snapshots --test kitty_e2e --test mcp_smoke
      - name: PTY-driven tests (unix only)
        run: cargo test --all-features --test smoke --test vtty_tui

